// @bgm/ui — librería de componentes base + tokens del motor.
// Patrón: el paquete apunta a `src` (sin build); el Vite de cada app
// compila los SFC del workspace. Replica el enfoque de @kontuan/shared.

export { default as BaseButton } from './components/BaseButton.vue'
export { default as MotorBadge } from './components/MotorBadge.vue'
