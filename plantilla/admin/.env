# URL base de la API del motor (patrón kontuan: el front la lee de aquí).
# Puerto 8010 para no chocar con el 8000 (CUPS / otras webs).
VITE_API_URL=http://localhost:8010/api

# URL de la web pública (enlace "ver la web" de la cabecera del admin).
VITE_APP_URL=http://localhost:5173
